import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Fashion & Design — The BeNetwork' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
